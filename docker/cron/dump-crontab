MAILTO=""

## Trigger a full dump on 1st and 15th of every month, near midnight
0 0 1,15 * * lbdumps /code/listenbrainz/admin/create-dumps.sh full >> /logs/dumps.log 2>&1
## Around 24 hours later, trigger a full import into the spark cluster
0 0 2,16 * * lbdumps /usr/local/bin/python /code/listenbrainz/manage.py spark request_import_full >> /logs/dumps.log 2>&1

## Trigger an incremental dump everyday, near noon, far away from whole dump times.
0 12 * * * lbdumps /code/listenbrainz/admin/create-dumps.sh incremental >> /logs/dumps.log 2>&1
## Around 1 hour later, trigger an incremental import into the spark cluster
0 13 * * * lbdumps /usr/local/bin/python /code/listenbrainz/manage.py spark request_import_incremental >> /logs/dumps.log 2>&1
